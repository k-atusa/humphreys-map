export interface BuildingData {
  buildingNumber: string;
  name: string;
  category: string;
  businessHours?: string;
  contact?: string;
  address?: string;
  latitude: number;
  longitude: number;
  description?: string;
}
